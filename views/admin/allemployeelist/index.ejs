<!DOCTYPE html>
<html>
  <head>
    <title> ALL EMPLOYEE LIST </title>
  </head>
  <body>
    <div style='width:100%;'>
    <form method='post'>
      <table style="width:100%;border: 2px, solid;" allign="middle">
        <thead>
          <td> SL </td>
          <td> ID </td>
          <td> Name </td>
          <td> Satus </td>
          <td> ACTION </td>
          <td> ACTION </td>
        </thead>

        <tbody>
            <% for(var i=0; i < list.length; i++){ %>
            <tr>
              <td><%= list[i].id %></td>
              <td><%= list[i].username %></td>
              <td><%= list[i].fname %></td>
              <td><%= list[i].status %></td>
              <td><a>UPDATE</a> </td>
              <td><a>DELETE</a></td>
            </tr>
            <% } %>
          </tbody>
      </table>
      </table>
    </div>

    </form>

  </body>
</html>
